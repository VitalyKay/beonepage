variables:
  GIT_SSL_NO_VERIFY: "true"

before_script:
  - git config --global user.email "vitalykay@gmail.com"
  - git config --global user.name "vitalykay"

deploy: 
  stage: deploy
  script: 
    - git clone git@gitlab:testproj/beonepage.git
    - scp -r ~/beonepage wordpress:/var/www/html/www.vitalykay-netology-diplom.tk/wp_content
  only:
    - master
    - tags
